<Window x:Class="PortManager.Views.DashBoardWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PortManager.Views"
        mc:Ignorable="d"
        Title="DashBoardWindow" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="10*"/>
            <RowDefinition Height="90*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10">

            <TextBox BorderBrush="Black" Height="20" Width="100" Text="search" />
            <Button Content="Refresh" Height="20" Margin="10,0"/>

        </StackPanel>
        
        <StackPanel Grid.Row="1" Orientation="Vertical">
            <DataGrid Margin="10" >
                
            </DataGrid>
        </StackPanel>
    </Grid>
</Window>
