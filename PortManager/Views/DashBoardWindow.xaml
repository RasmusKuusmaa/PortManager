<Window x:Class="PortManager.Views.DashBoardWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:PortManager.ViewModels"
        mc:Ignorable="d"
        Title="DashBoardWindow" Height="450" Width="800">
    <Window.DataContext>
        <vm:DashBoardViewModel/>
    </Window.DataContext>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="10*"/>
            <RowDefinition Height="90*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10">

            <TextBox BorderBrush="Black" Height="20" Width="100" Text="search" />
            <Button Content="Refresh" Height="20" Margin="10,0"/>

        </StackPanel>
        
        <StackPanel Grid.Row="1" Orientation="Vertical" Height="350" CanVerticallyScroll="True">
            <DataGrid ItemsSource="{Binding Ports}" AutoGenerateColumns="False" IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="PID" Binding="{Binding PID}" Width="*"/>
                    <DataGridTextColumn Header="Port" Binding="{Binding Port}" Width="*"/>
                    <DataGridTextColumn Header="Protocol" Binding="{Binding Protocol}" Width="*"/>
                    <DataGridTextColumn Header="Process Name" Binding="{Binding ProcessName}" Width="*"/>
                    <DataGridTextColumn Header="Local Address" Binding="{Binding LocalAddress}" Width="*"/>
                    <DataGridTextColumn Header="State" Binding="{Binding State}" Width="*"/>
                    <DataGridTemplateColumn Header="Action">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="Kill"
                                    Command="{Binding DataContext.KillCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                    CommandParameter="{Binding PID}"
                                    Background="Red" Foreground="White"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>

        </StackPanel>
    </Grid>
</Window>
