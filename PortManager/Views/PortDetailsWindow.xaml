<Window x:Class="PortManager.Views.PortDetailsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PortManager.Views"
        xmlns:conv="clr-namespace:PortManager.Converters"
        mc:Ignorable="d"
        Title="Details" Height="350" Width="350">
    <Window.Resources>
        <conv:NaToVisibilityConverter x:Key="NaToVisibilityConverter"/>
        <conv:NaToInvertedVisibilityConverter  x:Key="NaToInvertedVisibilityConverter"/>
    </Window.Resources>
    <ScrollViewer>
        <StackPanel Margin="10">
            <TextBlock Text="{Binding ProcessName}" FontWeight="Bold" FontSize="16"/>
            <TextBlock Text="Executable Path:"/>
            <StackPanel>
                <TextBlock TextWrapping="Wrap" Visibility="{Binding ExecutablePath, Converter={StaticResource NaToVisibilityConverter}}">
        <Hyperlink NavigateUri="{Binding ExecutablePath}" RequestNavigate="ExecutablePath_RequestNavigate">
            <Run Text="{Binding ExecutablePath}" />
        </Hyperlink>
                </TextBlock>

                <TextBlock Text="{Binding ExecutablePath}" TextWrapping="Wrap" Visibility="{Binding ExecutablePath, Converter={StaticResource NaToInvertedVisibilityConverter}}" />
            </StackPanel>

            <TextBlock Text="Start Time:"/>
            <TextBlock Text="{Binding StartTime}"/>
            <TextBlock Text="Memory (MB):"/>
            <TextBlock Text="{Binding MemoryMB}"/>
            <Button Content="Close" Width="100" Margin="0,10,0,0" Click="Close_Click" HorizontalAlignment="Left"/>
            <Button Content="Kill" Width="100" Margin="0,10,0,0" HorizontalAlignment="Left"
                Command="{Binding KillCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                CommandParameter="{Binding PID}"
                Background="Red"
                Foreground="White"/>
        </StackPanel>
    </ScrollViewer>
</Window>
